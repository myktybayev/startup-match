<?xml version="1.0" encoding="utf-8"?>
<androidx.constraintlayout.widget.ConstraintLayout xmlns:android="http://schemas.android.com/apk/res/android"
    xmlns:app="http://schemas.android.com/apk/res-auto"
    xmlns:tools="http://schemas.android.com/tools"
    android:layout_width="match_parent"
    android:layout_height="match_parent"
    android:background="@color/page_background"
    tools:context=".ui.investors.InvestorsFragment">

    <LinearLayout
        android:id="@+id/layoutInvestorsHeader"
        android:layout_width="0dp"
        android:layout_height="wrap_content"
        android:gravity="center_vertical"
        android:orientation="horizontal"
        android:paddingStart="20dp"
        android:paddingTop="16dp"
        android:paddingEnd="20dp"
        android:paddingBottom="16dp"
        app:layout_constraintEnd_toEndOf="parent"
        app:layout_constraintStart_toStartOf="parent"
        app:layout_constraintTop_toTopOf="parent">

        <LinearLayout
            android:layout_width="0dp"
            android:layout_height="wrap_content"
            android:layout_weight="1"
            android:orientation="vertical"
            android:paddingEnd="12dp">

            <TextView
                android:id="@+id/textInvestorsTitle"
                android:layout_width="match_parent"
                android:layout_height="wrap_content"
                android:fontFamily="@font/inter_bold"
                android:text="@string/title_dashboard"
                android:textColor="@color/investor_title"
                android:textSize="20sp" />

            <TextView
                android:id="@+id/textInvestorsSubtitle"
                android:layout_width="match_parent"
                android:layout_height="wrap_content"
                android:layout_marginTop="4dp"
                android:fontFamily="@font/inter_light"
                android:text="@string/investors_page_subtitle"
                android:textColor="@color/investor_text_secondary"
                android:textSize="12sp" />
        </LinearLayout>

        <ImageButton
            android:id="@+id/buttonInvestorsSearch"
            android:layout_width="40dp"
            android:layout_height="40dp"
            android:background="@drawable/bg_investors_icon_circle"
            android:contentDescription="@string/investors_search"
            android:padding="10dp"
            android:scaleType="centerInside"
            android:src="@drawable/ic_investors_search"
            android:tint="@null" />

        <ImageButton
            android:id="@+id/buttonInvestorsFilter"
            android:layout_width="40dp"
            android:layout_height="40dp"
            android:layout_marginStart="8dp"
            android:background="@drawable/bg_investors_icon_circle"
            android:contentDescription="@string/investors_filter"
            android:padding="10dp"
            android:scaleType="centerInside"
            android:src="@drawable/ic_investors_filter"
            android:tint="@null" />
    </LinearLayout>

    <HorizontalScrollView
        android:id="@+id/scrollInvestorFilters"
        android:layout_width="0dp"
        android:layout_height="wrap_content"
        android:background="@color/page_background"
        android:clipToPadding="false"
        android:fillViewport="false"
        android:paddingTop="12dp"
        android:paddingBottom="12dp"
        android:scrollbars="none"
        app:layout_constraintEnd_toEndOf="parent"
        app:layout_constraintStart_toStartOf="parent"
        app:layout_constraintTop_toBottomOf="@id/layoutInvestorsHeader">

        <LinearLayout
            android:id="@+id/layoutFilterChips"
            android:layout_width="wrap_content"
            android:layout_height="wrap_content"
            android:orientation="horizontal"
            android:paddingStart="20dp"
            android:paddingEnd="20dp" />
    </HorizontalScrollView>

    <View
        android:id="@+id/dividerInvestorFilters"
        android:layout_width="0dp"
        android:layout_height="1dp"
        android:background="@color/investor_filter_divider"
        app:layout_constraintEnd_toEndOf="parent"
        app:layout_constraintStart_toStartOf="parent"
        app:layout_constraintTop_toBottomOf="@id/scrollInvestorFilters" />

    <androidx.recyclerview.widget.RecyclerView
        android:id="@+id/recyclerInvestors"
        android:layout_width="0dp"
        android:layout_height="0dp"
        android:clipToPadding="false"
        android:paddingStart="20dp"
        android:paddingTop="16dp"
        android:paddingEnd="20dp"
        android:paddingBottom="56dp"
        app:layout_constraintBottom_toBottomOf="parent"
        app:layout_constraintEnd_toEndOf="parent"
        app:layout_constraintStart_toStartOf="parent"
        app:layout_constraintTop_toBottomOf="@id/dividerInvestorFilters"
        tools:listitem="@layout/item_investor_card" />

</androidx.constraintlayout.widget.ConstraintLayout>
